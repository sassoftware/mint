if [ $PYTHONPATH ]; then
    PYTHONPATH=$PYTHONPATH:/var/mailman
else
    PYTHONPATH=/var/mailman
fi

LOC=/var/mailman/bin
CONF=/var/mailman/bin/fixowners.conf

PYTHONPATH=$PYTHONPATH python ${LOC}/fixowners.py -C ${CONF}
